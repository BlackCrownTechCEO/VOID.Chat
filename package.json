{
  "name": "void-full-encrypted-protocol",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix shared install && npm --prefix backend install && npm --prefix frontend install && npm --prefix mobile install",
    "dev:backend": "npm --prefix backend run dev",
    "dev:web": "npm --prefix frontend run dev",
    "build:web": "npm --prefix frontend run build"
  }
}
